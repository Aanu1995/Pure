// This enum is used to show the user connection status with another user
// You might ask what is the difference between PENDING and ACCEPT
// When the inviter views the invitee profile it shows PENDING but when the
// the invitee views the inviter profile it shows ACCEPT
enum ConnectionAction { CONNECT, PENDING, ACCEPT, MESSAGE }
